{
  "name": "salesforce-deployment-guard",
  "displayName": "SF Guard",
  "description": "Prevents accidental code overwrites during Salesforce deployments with conflict detection and visual merge tools",
  "version": "1.0.0",
  "publisher": "SubhadeepDev",
  "icon": "images/sf-guard-production-logo.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/CR-Samrat/salesforce-deployment-guard"
  },
  "keywords": [
    "salesforce",
    "deployment",
    "conflict detection",
    "apex",
    "lwc",
    "sfdx",
    "sf",
    "guard",
    "merge",
    "diff",
    "code protection"
  ],
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "salesforce-deployment-guard.retrieve",
        "title": "SF Guard: Retrieve from Org",
        "icon": "$(cloud-download)"
      },
      {
        "command": "salesforce-deployment-guard.safeDeploy",
        "title": "SF Guard: Safe Deploy to Org",
        "icon": "$(shield)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "salesforce-deployment-guard.retrieve",
          "when": "resourceExtname == .cls || resourceExtname == .trigger || resourceExtname == .apex || resourceExtname == .html || resourceExtname == .js || resourceExtname == .css",
          "group": "salesforce@1"
        },
        {
          "command": "salesforce-deployment-guard.safeDeploy",
          "when": "resourceExtname == .cls || resourceExtname == .trigger || resourceExtname == .apex || resourceExtname == .html || resourceExtname == .js || resourceExtname == .css",
          "group": "salesforce@2"
        }
      ],
      "editor/context": [
        {
          "command": "salesforce-deployment-guard.retrieve",
          "when": "resourceExtname == .cls || resourceExtname == .trigger || resourceExtname == .apex || resourceExtname == .html || resourceExtname == .js || resourceExtname == .css",
          "group": "salesforce@1"
        },
        {
          "command": "salesforce-deployment-guard.safeDeploy",
          "when": "resourceExtname == .cls || resourceExtname == .trigger || resourceExtname == .apex || resourceExtname == .html || resourceExtname == .js || resourceExtname == .css",
          "group": "salesforce@2"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.48.1",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
